{
  "name": "blueprint-buddy",
  "version": "2.0.0",
  "description": "AI-guided parametric furniture design studio — chat proposes intent, code owns the geometry",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node test/unit.test.js",
    "test:smoke": "node build.js && node test/smoke.playwright.js"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
